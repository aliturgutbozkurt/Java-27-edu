# Compiled class files
*.class

# Log files
*.log

# Editor and OS cruft
.DS_Store
.idea/
.vscode/
*.iml
*.swp

# Claude Code session-local state, not project config
.claude/scheduled_tasks.lock

# PDFs are committed so they can be downloaded from GitHub without a local
# toolchain. They are generated output: rebuild with ./scripts/build-pdfs.sh
# after editing any Markdown, and commit the result.
